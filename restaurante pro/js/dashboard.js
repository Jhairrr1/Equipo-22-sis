function renderDashboard(){const pedidos=DB.get(DB_KEYS.PEDIDOS)||[],mesas=DB.get(DB_KEYS.MESAS)||[],pagos=DB.get(DB_KEYS.PAGOS)||[],menu=DB.get(DB_KEYS.MENU)||[];const ventas=pagos.reduce((a,p)=>a+p.monto,0),activos=pedidos.filter(p=>['Pendiente','En preparación','Listo','Entregado'].includes(p.estado)).length,ocupadas=mesas.filter(m=>m.estado==='Ocupada').length,ticket=pagos.length?ventas/pagos.length:0;document.getElementById('kpis').innerHTML=`<div class="kpi"><div class="icon">💰</div><div class="label">Ventas</div><div class="value">${fmt(ventas)}</div><small>${pagos.length} pagos</small></div><div class="kpi"><div class="icon">📝</div><div class="label">Pedidos activos</div><div class="value">${activos}</div><small>${pedidos.length} totales</small></div><div class="kpi"><div class="icon">🪑</div><div class="label">Mesas ocupadas</div><div class="value">${ocupadas}/${mesas.length}</div><small>Mesas registradas</small></div><div class="kpi"><div class="icon">🎫</div><div class="label">Ticket promedio</div><div class="value">${fmt(ticket)}</div><small>Promedio por pago</small></div>`;const c={};pedidos.forEach(p=>p.items.forEach(i=>c[i.nombre]=(c[i.nombre]||0)+i.cant));const top=Object.entries(c).sort((a,b)=>b[1]-a[1]).slice(0,6),max=Math.max(...top.map(x=>x[1]),1);document.getElementById('topPlatos').innerHTML=top.length?`<div class="bars">${top.map(([n,v])=>`<div class="bar-row"><div>${n}</div><div class="bar-track"><div class="bar-fill" style="width:${v/max*100}%"></div></div><b>${v} uds</b></div>`).join('')}</div>`:'<div class="empty">Sin pedidos aún</div>';const met={};pagos.forEach(p=>met[p.metodo]=(met[p.metodo]||0)+p.monto);document.getElementById('porMetodo').innerHTML=Object.keys(met).length?`<div class="legend">${Object.entries(met).map(([m,v])=>`<div class="legend-item">${estadoBadge(m)}<div style="flex:1"></div><b>${fmt(v)}</b></div>`).join('')}</div>`:'<div class="empty">Sin pagos registrados</div>';document.getElementById('ultPedidos').innerHTML=pedidos.length?`<table class="tbl"><thead><tr><th>#</th><th>Mesa</th><th>Total</th><th>Estado</th><th>Fecha</th></tr></thead><tbody>${[...pedidos].reverse().slice(0,8).map(p=>`<tr><td><b>#${p.id}</b></td><td>Mesa ${p.mesaId}</td><td><b>${fmt(p.total)}</b></td><td>${estadoBadge(p.estado)}</td><td>${shortDate(p.fecha)}</td></tr>`).join('')}</tbody></table>`:'<div class="empty">Sin pedidos registrados</div>'}
